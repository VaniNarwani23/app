{
  "scripts": {
    "start": "npm run dev",
    "dev": "yarn --cwd frontend start",
    "build": "yarn --cwd frontend build"
  },
  "dependencies": {
    "typed.js": "^2.1.0"
  },
  "devDependencies": {
    "@types/stack-utils": "^2.0.3"
  }
}
